import { CanScanDirective } from './can-scan.directive';

describe('CanScanDirective', () => {
  it('should create an instance', () => {
    const directive = new CanScanDirective();
    expect(directive).toBeTruthy();
  });
});
